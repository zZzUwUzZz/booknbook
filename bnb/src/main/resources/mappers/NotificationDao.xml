<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjcs.bnb.dao.NotificationDao">

    <insert id="addToNotifboard">
        INSERT INTO NOTIFBOARD(nb_m_id, nb_msg, nb_date, nb_read, b_isbn, b_s_id, b_title, s_storename) 
        VALUES(#{c_id}, #{sort}, DEFAULT, 'N', #{isbn}, #{s_id}, #{title}, #{storename})
    </insert>

</mapper>